@model IEnumerable<WebApplicationMVC.ArticleType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ArticleTypeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ArticleCounts)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DelFlag)
        </th>
        <th></th>
    </tr>

@foreach (ArticleType item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ArticleTypeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ArticleCounts)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DelFlag)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
